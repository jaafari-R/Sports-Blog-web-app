extends ../layout 


block content
    .container
        h1 #{title}
        ul.list-group
            each category, i in categories
                li.list-group-item
                    a(href=`/categories/${category._id}`) #{category.title}